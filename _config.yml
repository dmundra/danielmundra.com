# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you:
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: Daniel Mundra
email: projects@danielmundra.com
description: >- # this means to ignore newlines until "baseurl:"
  About, projects and contact information for Daniel Mundra.
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://danielmundra.com" # the base hostname & protocol for your site, e.g. http://example.com
github_username: dmundra
atom_feed:
  hide: true

# Author Settings
author:
  name: "Daniel Mundra"
  bio: "I create and manage custom Drupal, HTML/CSS, and WordPress sites."
  links:
    - label: "Drupal"
      icon: "fab fa-fw fa-drupal"
      url: "https://drupal.org/u/dmundra"
    - label: "GitHub"
      icon: "fab fa-fw fa-github"
      url: "https://github.com/dmundra"

# Build settings
remote_theme: mmistakes/minimal-mistakes
minimal_mistakes_skin: "default"

plugins:
  - jekyll-remote-theme
  - jekyll-include-cache
  - jekyll-redirect-from
  - jekyll-sitemap

# Analytics
analytics:
  provider: "google-gtag"
  google:
    tracking_id: "G-20CD9NKQ9N"

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
exclude:
  - vendor
  - Gemfile
  - Gemfile.lock
  - node_modules
  - .config
  - package.json
  - package-lock.json
  - .gitignore
  - .github
  - .pa11yci
  - pa11y_output.txt
  - .tugboat
  - LICENSE
  - .pre-commit-config.yaml
  - CNAME
